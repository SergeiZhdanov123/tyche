# Backend (already running on port 8000)
cd backend && source venv/bin/activate && uvicorn api:app --reload

# Frontend
cd tyche-terminal && npm run dev

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tychefinancials@gmail.com
SMTP_PASS=iaqj jsor skib kman
NOTIFY_EMAIL=tychefinancials@gmail.com
